This is a chrome extensions that changes `<img/>` sources to a black image.
It also removes all the `<iframe>` elements.
You should refresh the page after enabling it.
It's pretty useless to be honest.
